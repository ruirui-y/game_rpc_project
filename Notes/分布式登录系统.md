# 思路
目标是一个网关一个登录server



# 创建文件夹
创建好之后

```bash
:: 1. 创建并进入项目根目录
mkdir game_rpc_project
cd game_rpc_project

:: 2. 创建各个子模块目录
mkdir rpc_core\include
mkdir rpc_core\src
mkdir proto
mkdir pb
mkdir gateway_server
mkdir login_server

:: 3. 创建基础的空文件 (Windows 下相当于 touch)
type nul > CMakeLists.txt
type nul > proto\login.proto
type nul > proto\rpcheader.proto
type nul > gateway_server\main.cpp
type nul > login_server\main.cpp
```





# 构建CMake
```cmake
cmake_minimum_required(VERSION 3.10)
project(GameRpcProject)

# 设置 C++ 标准
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# 全局查找 Protobuf
find_package(Protobuf REQUIRED)

# 设置全局头文件搜索路径 (让所有子目录都能找到 pb 和 rpc_core 的头文件)
include_directories(
    ${PROJECT_SOURCE_DIR}/rpc_core/include
    ${PROJECT_SOURCE_DIR}/pb
    ${Protobuf_INCLUDE_DIRS}
)

# 依次添加子目录 (注意顺序：先编译底层的 rpc_core，再编译上层服务)
add_subdirectory(rpc_core)
add_subdirectory(login_server)
add_subdirectory(gateway_server)
```



编译proto，并链接cpp和静态库

```cmake
# ==========================================
# 专门负责 rpc_core 静态库的编译和 proto 生成
# ==========================================

set(PROTO_SRC_DIR ${CMAKE_SOURCE_DIR}/proto)
set(PB_OUT_DIR ${CMAKE_SOURCE_DIR}/pb)

# 1. 找到所有的 .proto 文件
file(GLOB PROTO_FILES "${PROTO_SRC_DIR}/*.proto")

# 2. 预期生成的文件
set(PROTO_PCH_SRCS
    ${PB_OUT_DIR}/login.pb.cc
    ${PB_OUT_DIR}/rpcheader.pb.cc
)
set(PROTO_PCH_HDRS
    ${PB_OUT_DIR}/login.pb.h
    ${PB_OUT_DIR}/rpcheader.pb.h
)

# 3. 自定义命令：执行 protoc
add_custom_command(
    OUTPUT ${PROTO_PCH_SRCS} ${PROTO_PCH_HDRS}
    COMMAND ${Protobuf_PROTOC_EXECUTABLE}
    ARGS --cpp_out=${PB_OUT_DIR} -I${PROTO_SRC_DIR} ${PROTO_FILES}
    DEPENDS ${PROTO_FILES}
    COMMENT "正在使用 protoc 编译 .proto 文件到 pb/ 目录..."
)

# 4. 获取 rpc_core/src 下的源码 (目前可能为空，没关系)
file(GLOB RPC_CORE_SRCS "${CMAKE_CURRENT_SOURCE_DIR}/src/*.cpp")

# 5. 生成静态库 rpc_core
add_library(rpc_core STATIC ${RPC_CORE_SRCS} ${PROTO_PCH_SRCS})

# 6. 链接依赖的底层库：Protobuf、pthread、以及你之前写的 mymuduo
target_link_libraries(rpc_core PUBLIC ${Protobuf_LIBRARIES} pthread mymuduo)
```



```cmake
# ==========================================
# 登录服务节点
# ==========================================

# 搜集当前目录下的源码
file(GLOB LOGIN_SRCS "*.cpp")

# 生成可执行文件
add_executable(login_server ${LOGIN_SRCS})

# 链接你刚刚写的 rpc_core 库
target_link_libraries(login_server rpc_core)
```

```cmake
# ==========================================
# 网关服务节点
# ==========================================

# 搜集当前目录下的源码
file(GLOB GATEWAY_SRCS "*.cpp")

# 生成可执行文件
add_executable(gateway_server ${GATEWAY_SRCS})

# 链接 rpc_core 库
target_link_libraries(gateway_server rpc_core)
```



# RPC接口
```protobuf
syntax = "proto3";

// 定义命名空间，生成的 C++ 代码会被包裹在 game::rpc 这个 namespace 下
package game.rpc;

// 【核心设置】告诉 protoc 编译器：帮我生成 C++ 的纯虚基类（RPC 桩）
// 你的 RPC 框架就是要去继承并重写这些虚函数！
option cc_generic_services = true;

// 1. 定义登录请求的数据结构
message LoginRequest 
{
    string username = 1;                                                        // 账号
    string password = 2;                                                        // 密码
}

// 2. 定义登录响应的数据结构
message LoginResponse 
{
    int32 errcode = 1;                                                          // 错误码：0表示成功，非0表示具体的错误类型
    string errmsg = 2;                                                          // 错误信息：例如 "密码错误"、"账号不存在"
    string token = 3;                                                           // 登录凭证：成功后发给客户端，后续请求带上这个证明身份
}

// 3. 定义服务接口
service LoginService 
{
    // 声明一个 RPC 方法：接收 LoginRequest，返回 LoginResponse
    rpc Login(LoginRequest) returns(LoginResponse);
}
```



```protobuf
syntax = "proto3";

// 生成的 C++ 代码会被包裹在 rpc::core 这个 namespace 下
package rpc.core;

// RPC 消息头结构
message RpcHeader 
{
    string service_name = 1;                                    // 服务对象名。例如："game.rpc.LoginService"
    string method_name = 2;                                     // 具体的函数名。例如："Login"
    uint32 args_size = 3;                                       // 重点：紧跟在 Header 后面的参数 (比如 LoginRequest) 序列化后的字节长度！
}
```



至此，用vs打开文件夹，同步到远程框架中

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/54962239/1773389857175-11242174-4a2a-4aed-ae81-7c894e29f138.png)



# RPC流程
这里很明显是服务端要继承的类，因为CallMethod会根据方法描述自动的调用方法

```cpp
class LoginService : public ::PROTOBUF_NAMESPACE_ID::Service {
protected:
// This class should be treated as an abstract interface.
inline LoginService() {};
public:
virtual ~LoginService();

typedef LoginService_Stub Stub;

static const ::PROTOBUF_NAMESPACE_ID::ServiceDescriptor* descriptor();

virtual void Login(::PROTOBUF_NAMESPACE_ID::RpcController* controller,
const ::game::rpc::LoginRequest* request,
::game::rpc::LoginResponse* response,
::google::protobuf::Closure* done);

// implements Service ----------------------------------------------

const ::PROTOBUF_NAMESPACE_ID::ServiceDescriptor* GetDescriptor();
void CallMethod(const ::PROTOBUF_NAMESPACE_ID::MethodDescriptor* method,
::PROTOBUF_NAMESPACE_ID::RpcController* controller,
const ::PROTOBUF_NAMESPACE_ID::Message* request,
::PROTOBUF_NAMESPACE_ID::Message* response,
::google::protobuf::Closure* done);
const ::PROTOBUF_NAMESPACE_ID::Message& GetRequestPrototype(
const ::PROTOBUF_NAMESPACE_ID::MethodDescriptor* method) const;
const ::PROTOBUF_NAMESPACE_ID::Message& GetResponsePrototype(
const ::PROTOBUF_NAMESPACE_ID::MethodDescriptor* method) const;

private:
GOOGLE_DISALLOW_EVIL_CONSTRUCTORS(LoginService);
};

LoginService::~LoginService() {}

const ::PROTOBUF_NAMESPACE_ID::ServiceDescriptor* LoginService::descriptor() {
  ::PROTOBUF_NAMESPACE_ID::internal::AssignDescriptors(&descriptor_table_login_2eproto);
  return file_level_service_descriptors_login_2eproto[0];
}

const ::PROTOBUF_NAMESPACE_ID::ServiceDescriptor* LoginService::GetDescriptor() {
  return descriptor();
}

void LoginService::Login(::PROTOBUF_NAMESPACE_ID::RpcController* controller,
                         const ::game::rpc::LoginRequest*,
                         ::game::rpc::LoginResponse*,
                         ::google::protobuf::Closure* done) {
  controller->SetFailed("Method Login() not implemented.");
  done->Run();
}

void LoginService::CallMethod(const ::PROTOBUF_NAMESPACE_ID::MethodDescriptor* method,
                             ::PROTOBUF_NAMESPACE_ID::RpcController* controller,
                             const ::PROTOBUF_NAMESPACE_ID::Message* request,
                             ::PROTOBUF_NAMESPACE_ID::Message* response,
                             ::google::protobuf::Closure* done) {
  GOOGLE_DCHECK_EQ(method->service(), file_level_service_descriptors_login_2eproto[0]);
  switch(method->index()) {
    case 0:
      Login(controller,
             ::PROTOBUF_NAMESPACE_ID::internal::DownCast<const ::game::rpc::LoginRequest*>(
                 request),
             ::PROTOBUF_NAMESPACE_ID::internal::DownCast<::game::rpc::LoginResponse*>(
                 response),
             done);
      break;
    default:
      GOOGLE_LOG(FATAL) << "Bad method index; this should never happen.";
      break;
  }
}
```



这里用到了channel，是和发送方相关的，很显然需要继承RpcChannel，然后重载发送方法CallMethod

```cpp
class LoginService_Stub : public LoginService {
 public:
  LoginService_Stub(::PROTOBUF_NAMESPACE_ID::RpcChannel* channel);
  LoginService_Stub(::PROTOBUF_NAMESPACE_ID::RpcChannel* channel,
                   ::PROTOBUF_NAMESPACE_ID::Service::ChannelOwnership ownership);
  ~LoginService_Stub();

  inline ::PROTOBUF_NAMESPACE_ID::RpcChannel* channel() { return channel_; }

  // implements LoginService ------------------------------------------

  void Login(::PROTOBUF_NAMESPACE_ID::RpcController* controller,
                       const ::game::rpc::LoginRequest* request,
                       ::game::rpc::LoginResponse* response,
                       ::google::protobuf::Closure* done);
 private:
  ::PROTOBUF_NAMESPACE_ID::RpcChannel* channel_;
  bool owns_channel_;
  GOOGLE_DISALLOW_EVIL_CONSTRUCTORS(LoginService_Stub);
};

LoginService_Stub::LoginService_Stub(::PROTOBUF_NAMESPACE_ID::RpcChannel* channel)
  : channel_(channel), owns_channel_(false) {}
LoginService_Stub::LoginService_Stub(
    ::PROTOBUF_NAMESPACE_ID::RpcChannel* channel,
    ::PROTOBUF_NAMESPACE_ID::Service::ChannelOwnership ownership)
  : channel_(channel),
    owns_channel_(ownership == ::PROTOBUF_NAMESPACE_ID::Service::STUB_OWNS_CHANNEL) {}
LoginService_Stub::~LoginService_Stub() {
  if (owns_channel_) delete channel_;
}

void LoginService_Stub::Login(::PROTOBUF_NAMESPACE_ID::RpcController* controller,
                              const ::game::rpc::LoginRequest* request,
                              ::game::rpc::LoginResponse* response,
                              ::google::protobuf::Closure* done) {
  channel_->CallMethod(descriptor()->method(0),
                       controller, request, response, done);
}
```



# 网关服务
## 发送逻辑
发送的包格式为LEN + RpcHeader + RpcRequest

接收的包格式为LEN + RpcResponse

所以需要先序列化请求获取参数长度，然后填充RcpHeader，组装发送包，阻塞式发送，解析响应包，并填充到response中

```cpp
void Channel::CallMethod(const google::protobuf::MethodDescriptor* method,
    google::protobuf::RpcController* controller,
    const google::protobuf::Message* request,
    google::protobuf::Message* response,
    google::protobuf::Closure* done)
{
    // 1. 获取服务，方法名以及参数长度
    const google::protobuf::ServiceDescriptor* service_descrip = method->service();
    string service_name = service_descrip->name();
    string method_name = method->name();
    int param_size = request->ByteSize();

    // 2. 构造请求头
    rpc::core::RpcHeader header;
    header.set_service_name(method->service()->name());
    header.set_method_name(method->name());
    header.set_args_size(param_size);

    // 3. 序列化请求头和请求数据
    std::string header_str;
    header.SerializeToString(&header_str);
    uint32_t header_size = header_str.size();

    std::string request_str;
    request->SerializeToString(&request_str);

    // 5. 数据整合
    std::string send_rpc_str;
    send_rpc_str.insert(0, std::string((char*)&header_size, 4));
    send_rpc_str += header_str;
    send_rpc_str += request_str;

    // 6. 复用连接并发送数据
    int sockfd = ConnectionPool::GetInstance().GetConnection("127.0.0.1", 9090);
    if (-1 == send(sockfd, send_rpc_str.c_str(), send_rpc_str.size(), 0))                             // 发送二进制流
    {
        close(sockfd);                                                                                // 失败关闭
        if (controller) controller->SetFailed("send error! errno:" + std::to_string(errno));          // 记录失败
        return;
    }

    // 7. 接收响应头
    uint32_t response_size;
    int offset = 0;
    while (offset < 4)
    {
        int n = recv(sockfd, (char*)&response_size + offset, 4 - offset, 0);
        if (n <= 0)
        {
            // 连接可能断了，按你之前的逻辑处理
            close(sockfd);
            return;
        }
        offset += n;
    }

    if (response_size > 10 * 1024 * 1024)
    {
        if (controller) controller->SetFailed("response is too big!");
        close(sockfd);
        return;
    }


    // 8. 接收响应体
    std::string response_data;
    response_data.resize(response_size);
    uint32_t total_recv = 0;

    // 设置超时事件
    struct timeval timeout = { 5, 0 };
    setsockopt(sockfd, SOL_SOCKET, SO_RCVTIMEO, (char*)&timeout, sizeof(struct timeval));
    while (total_recv < response_size)
    {
        // 往指定的位置写入指定大小的数据
        int n = recv(sockfd, &response_data[total_recv], response_size - total_recv, 0);
        // 超时
        if (n < 0)
        {
            if (errno == EAGAIN || errno == EWOULDBLOCK)
            {
                if (controller) controller->SetFailed("RPC recv timeout!");
            }
            else
            {
                if (controller) controller->SetFailed("recv error! errno:" + std::to_string(errno));
            }
            close(sockfd);
            return;
        }
        // 连接断开
        else if (n == 0)
        {
            if (controller) controller->SetFailed("server connection closed unexpectedly!");
            close(sockfd);
            return;
        }
        total_recv += n;
    }

    // 9. 反序列化响应数据
    if (!response->ParseFromString(response_data))                                                      // 解析并填充
    {
        close(sockfd);                                                                                  // 失败关闭
        if (controller) controller->SetFailed("parse error! response_str:" + response_data);            // 解析失败
        return;
    }
}
```



## 发送登录请求
```cpp
#include <iostream>
#include "Channel.h"
#include "Log/Logger.h"
#include "login.pb.h"

int main(int argc, char** argv) 
{
    Channel channel;
    LOG_INFO << "=== Gateway Server is starting ===";
    
    game::rpc::LoginService_Stub stub(&channel);
    game::rpc::LoginRequest request;
    game::rpc::LoginResponse response;
    request.set_username("test");
    request.set_password("123456");
    stub.Login(nullptr, &request, &response, nullptr);
    string success = response.errcode() == 0 ? "true" : "false";
    LOG_INFO << "login request " << success;
    return 0;
}
```



# 登录服务
主要设计是需要监听服务发来的rpc请求

由于是网关服务是长连接，所以直接需要把server拉起来就好了，而且只有少数几个连接，没必要用线程池



RPCServer负责解析消息，从RpcHeader中获取服务名，根据服务名获取对应的服务，调用服务下的CallMethod方法

根据服务名获取服务需要创建一个映射

CallMethod又需要5个参数，首先是method，只能根据服务描述来获取

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/54962239/1773397762403-d89910ad-5a93-47c5-970d-00e563ea9662.png)



难的是如何获取索引，毕竟不是通过方法名获取的，而我们请求头的只能获取方法名，只有两种办法，第一种保留方法和方法名的映射，第二种请求头里面直接放方法索引，为了图省事，直接放方法索引

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/54962239/1773398393587-398dec39-75fd-4569-9aec-aba71351c910.png)

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/54962239/1773397884674-4a2efaf0-0869-49b3-a091-edda97bf67e9.png)



总结来说逻辑就是根据服务名获取服务描述，这里需要一个map保留服务名和服务之间的映射，在然后拼凑参数，首先是方法描述，根据方法索引来获取

请求和响应的获取在服务中亦有记载，他们的参数就是我们之前说的方法描述

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/54962239/1773398643455-f0a66fe0-6866-4f54-b6c3-af561c03b89f.png)



最后一个参数是done，当然也就是处理完后的回调，闭着眼睛想肯定是把包发回去啊

所以知道了这些，那么RPCServer不是轻轻松松了？



RPCServcr写好之后是不需要做任何修改的，如果换成了注册服务，也只需要将注册服务注册到RPCServer即可



## RPCServer
这是头文件架构，变量也就是我们之前说的映射

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/54962239/1773454791371-1e9fb11a-75b1-4efa-895a-eb0ec0a9dcee.png)



这里为什么存储Service，因为Service下面既能获取描述，也能获取对应的请求和响应

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/54962239/1773454845080-f0c12196-d8c4-40f4-9008-a68b605d507a.png)

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/54962239/1773454870270-c337c3c3-197b-4a3d-b1c4-bcb67bf0f885.png)



可以看到我们的登录请求继承的其实是::PROTOBUF_NAMESPACE_ID::Message，我们只需要获取基类，把网络中接收到的数据写进去，在对应的rpc会动态的转化成各自的子类

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/54962239/1773455010557-c6c793b1-481a-4bb3-bfc6-5e47d8a96793.png)



我想我已经说的很清楚了，这里就不再继续阐述了，直接上源码

```cpp
#ifndef RPC_SERVER_H
#define RPC_SERVER_H

#include <iostream>
#include <string>
#include <unordered_map>
#include <mymuduo/net/TcpServer.h>
#include <google/protobuf/service.h>
#include <google/protobuf/descriptor.h>

class RPCServer 
{
public:
	RPCServer(std::string ip, uint16_t port)
		:ip_(ip), port_(port) {}

	void RegisterService(google::protobuf::Service* service);
	void Run();

private:
	void OnMessage(const std::shared_ptr<TcpConnection>& conn, Buffer* buffer);
	void SendRpcResponse(const std::shared_ptr<TcpConnection>& conn, google::protobuf::Message* response);

private:
	std::unordered_map<std::string, google::protobuf::Service*> services;
	EventLoop event_loop_;
	std::string ip_;
	uint16_t port_;
};

#endif // RPC_SERVER_H

#include "RPCServer.h"
#include "rpcheader.pb.h"
#include <mymuduo/Log/Logger.h>

void RPCServer::RegisterService(google::protobuf::Service* service)
{
	services[service->GetDescriptor()->name()] = service;
}

void RPCServer::Run()
{
	TcpServer server(&event_loop_, ip_, port_, 10);
	server.SetMessageCallback(
		[this](const std::shared_ptr<TcpConnection>& conn, Buffer* buffer)
		{
			OnMessage(conn, buffer);
		});

	server.Start(0);
	event_loop_.Loop();
}

void RPCServer::OnMessage(const std::shared_ptr<TcpConnection>& conn, Buffer* buffer)
{
    std::string recv_buf = buffer->RetrieveAllAsString();                                               

    // 1. 从字符流中读取前4个字节的内容
    uint32_t header_size = 0;                                                                           
    recv_buf.copy((char*)&header_size, 4, 0);                                                           

    // 2. 根据header_size读取数据头的原始字符流，反序列化数据
    std::string rpc_header_str = recv_buf.substr(4, header_size);                                       
    rpc::core::RpcHeader rpcHeader;                                                                     
    std::string service_name;                                                                           
    std::string method_name;                                                                            
    uint32_t method_index;                                                                              
    uint32_t args_size;                                                                                 

    if (rpcHeader.ParseFromString(rpc_header_str))                                                      
    {
        service_name = rpcHeader.service_name();                                                        
        method_name = rpcHeader.method_name();                                                          
        method_index = rpcHeader.method_index();
        args_size = rpcHeader.args_size();                                                              
    }
    else
    {
        LOG_ERROR << "rpc_header_str: " << rpc_header_str.c_str() << " parse error!";                                                                                                                                                                                                                                                                                                                                           // 记录解析失败
        return;
    }

    // 3. 获取rpc方法参数的二进制数据
    std::string args_str = recv_buf.substr(4 + header_size, args_size);                                 

    // 4. 获取service对象和method对象
    auto it = services.find(service_name);                                                              
    if (it == services.end())
    {
        LOG_ERROR << "%s is not exist! " << service_name.c_str();                                       
        return;
    }

    google::protobuf::Service* service = it->second;                                                    
    const google::protobuf::ServiceDescriptor* service_desc = service->GetDescriptor();                         
    const google::protobuf::MethodDescriptor* method = service_desc->method(method_index);              

    // 5. 生成rpc方法调用的请求request和响应response参数
    google::protobuf::Message* request = service->GetRequestPrototype(method).New();                    
    if (!request->ParseFromString(args_str))                                                            
    {
        LOG_ERROR << "request parse error, content: " << args_str.c_str();                              
        return;
    }
    google::protobuf::Message* response = service->GetResponsePrototype(method).New();                  

    // 6. 设置回调函数，将rpc响应发送给客户端
    google::protobuf::Closure* done = google::protobuf::NewCallback<RPCServer,
        const std::shared_ptr<TcpConnection>&,
        google::protobuf::Message*>
        (this, &RPCServer::SendRpcResponse, conn, response);

    // 7. 当前服务会根据method调用自己的rpc方法
    service->CallMethod(method, nullptr, request, response, done);
}

// 长度(4B) + 内容
void RPCServer::SendRpcResponse(const std::shared_ptr<TcpConnection>& conn, google::protobuf::Message* response)
{
    std::string response_str;
    if (response->SerializeToString(&response_str))
    {
        // --- 添加 4 字节长度报头 ---
        uint32_t response_size = response_str.size();
        std::string send_str;
        // 把长度信息以二进制形式放入前 4 个字节
        send_str.insert(0, std::string((char*)&response_size, 4));
        send_str += response_str;

        conn->Send(send_str);
    }
    else
    {
        LOG_ERROR << "serialize response_str error!";
    }
}
```





## 重载登录方法
因为我们的登录服务下只定义了这么一个方法，所以只需要把Login重载即可

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/54962239/1773455371324-9bd360e4-3cb1-4d27-aaa7-5f7d4f6a268f.png)

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/54962239/1773455361430-2b9a3371-bb26-4401-b522-a5bb12ccbad5.png)



请求和响应的强转在CallMethod中进行了

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/54962239/1773456165160-e8a20535-bbd6-42d4-a104-20837508d83c.png)



所以我们只需要重载Login方法然后调用本地的方法即可

```cpp
#ifndef MY_LOGIN_SERVICE_H
#define MY_LOGIN_SERVICE_H

#include <string>
#include "login.pb.h"
#include <mymuduo/Log/Logger.h>
using namespace game::rpc;

class MyLoginService : public game::rpc::LoginService
{
public:
    // 真正的本地业务逻辑
    bool Login(std::string name, std::string pwd)
    {
        LOG_INFO << "Execute Login Service";
        LOG_INFO << "user_name = " << name.c_str() << " pwd = " << pwd.c_str();
        return true;
    }

    virtual void Login(::google::protobuf::RpcController* controller,
        const ::LoginRequest* request,
        ::LoginResponse* response,
        ::google::protobuf::Closure* done)
    {
        // 1. 框架给业务吐出请求参数
        std::string name = request->username();
        std::string pwd = request->password();

        // 2. 执行本地业务
        bool login_result = Login(name, pwd);                                                       

        // 3. 把响应写回给框架
        response->set_errcode(login_result == true ? 0 : 1);
        response->set_errmsg("");                                                                   
        response->set_token("11111111");

        // 4. 执行回调操作
        // 这里的 done 本质是 RpcProvider 传进来的 SendRpcResponse 回调
        done->Run();                                                                                
    }
};

#endif
```



# 测试
可以看到，登录请求成功了

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/54962239/1773456770258-fc81b1b1-e907-4305-8a5f-bab99296a9fc.png)



查看登录服务的日志

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/54962239/1773456810259-e31e4f26-25da-4b75-9cd4-c845e22cefbb.png)



接下来就是连接断开了，因为这个网关服务我们没有阻塞

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/54962239/1773456845248-18c614ab-52bb-43ec-8976-c62910f498dd.png)

